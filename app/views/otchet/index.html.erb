<h1>Products</h1>

<p>
  Download:
  <%= link_to "CSV", otchet_path(format: "csv") %> |
  <%= link_to "Excel", otchet_path(format: "xls") %>
</p>

<table id="products">
  <tr>
    <th>Product Name</th>
    <th>Release Date</th>
    <th>Price</th>
  </tr>
<% @products.each do |product| %>
  <tr>
    <td><%= offer.id %></td>
    <td><%= offer.id %></td>
    <td><%= offer.id %></td>
  </tr>
<% end %>
</table>
